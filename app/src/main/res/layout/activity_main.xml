<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.jaipunjwani.androidpcbluetooth.MainActivity">

    <TextView
        android:text="Channel: "
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/channelTextView"
        tools:textSize="25sp" />

    <Space
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/channelTextView"/>

    <Button
        android:text="Subscribe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/subscribeButton"
        android:layout_below="@id/channelTextView" />

    <Button
        android:text="Publish"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/publishButton"
        android:layout_alignParentBottom="false"
        android:layout_below="@id/subscribeButton" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/messagesEditText"
        android:layout_below="@+id/publishButton"
        android:text="Messages Received:" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/statusEditText"
        android:text="Status Messages:"
        android:layout_below="@+id/messagesEditText" />

    <Button
        android:text="Launch Bluetooth Activity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/launchBluetoothButton"
        android:layout_below="@+id/statusEditText" />

</RelativeLayout>
